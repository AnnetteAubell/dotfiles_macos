
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
     ~/.zshrc:
     ~/.gitconfig:

- create:
    - ~/Work

- shell:
 # - [git submodule update --init --recursive, Installing submodules]
   - sleep 2
   - 
    command: echo It works
    stdout: true
    stderr: true
